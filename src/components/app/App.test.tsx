import React from 'react';

it('should say hello to the user', () => {
  // const { getByText } = render(<App />);
  // const element = getByText(/hello, Florin/i);
  // expect(element).toBeInTheDocument();
  expect(true).toBe(true);
});
